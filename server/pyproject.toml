[project]
name = "server"
version = "0.2.0"
description = "Relay from giko ashitav3 addon to google sheets. (HTTP -> HTTPS)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.12",
    "google-auth>=2.40.1",
    "gspread>=6.2.1",
    "pyinstaller>=6.13.0",
    "python-dateutil>=2.9.0.post0",
    "tzdata>=2025.2",
]
